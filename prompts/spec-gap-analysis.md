You are writing a **spec gap analysis**. The central question is **what exists in the implementation that the specifications never account for?** You are hunting **undocumented behavior**: capabilities, surfaces, flows, defaults, error paths, configuration, integrations, or constraints that show up in code (or in what ships) but **cannot be traced** to an outcome, requirement, acceptance criterion, or engineering spec that actually covers them. Shadow features, “you only learn it from reading the repo,” and spec debt that runs **from implementation toward documentation** all belong in this report.

Ground yourself in the project rules and where specifications live (product intent, acceptance criteria, engineering specs), and choose a scope for both **what code you will inspect** and **which spec corpus you treat as authoritative** for this pass (feature area, component, boundary layer, or similar).

If an earlier file claims to be this analysis, default path is **`spec-gap-analysis.md`** in the project root unless context says otherwise. Read it if present, but **every** claim is **unverified** until you have checked it against **current code and current specs**. Deliver one authoritative report: work from a fresh read of the implementation first, then use the old file only to verify, reject, or extend entries.

Walk the implementation with at least **two genuinely different lenses** (for example by user-visible behavior, by module or package boundaries, by data flow, by configuration and flags, or by public API and CLI). Combine or choose what best surfaces behavior the specs omit. For each candidate “extra,” ask whether any spec **clearly owns** it—by ID, by explicit prose, or by traceable link—not merely whether a generous reader could infer it. Stress-test your list: what pockets of code would a narrow tour miss, and what would a critic say you dismissed as “too small” but is still user- or operator-visible? Tighten or expand only when you find a real blind spot.

If you inherited a prior analysis, reconcile claim by claim against the current implementation and specifications. Keep verified and newly found gaps; remove or mark resolved what no longer holds, with a brief rationale when it helps.

Write to **`spec-gap-analysis.md`** in the project root unless context overrides. Each gap should give **where it lives in the implementation**, **what it is or does**, and **what is missing or insufficient in the spec** (no owner, no requirement, no acceptance testability, or outdated silence). Suggest what spec work would close the gap without changing your mandate to **describe** rather than **redesign** the product unless the gap implies a product decision. Note how you scoped and stress-tested. If you reconciled an older file, record what you verified, rejected, and added. If you find no gaps, say so and how you searched. Prioritize by risk, user impact, or operational surprise.

Call the analysis **done** when two framings were used, a critic pass happened, any prior file was treated as untrusted input, the written report is prioritized and actionable, and another iteration would not materially improve coverage. If scope is unclear or you cannot reach that bar, plan to fail as below—**do not** emit `<promise>` until the final line.

On the **last line**, output `<promise>SUCCESS</promise>` if you met the bar, otherwise `<promise>FAILURE</promise>` with a brief explanation.
